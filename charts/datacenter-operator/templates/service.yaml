apiVersion: v1
kind: Service
metadata:
  name: {{ include "datacenter-operator.fullname" . }}
  labels:
    {{- include "datacenter-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datacenter-operator.selectorLabels" . | nindent 4 }}
