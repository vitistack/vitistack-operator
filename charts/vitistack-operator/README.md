## Vitistack Operator Helm Chart

This Helm chart deploys the Vitistack Operator

### Installation

```
helm install vitistack-operator ./charts/vitistack-operator
```

See `values.yaml` for configurable parameters.
