---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: kubernetesproviders.vitistack.io
spec:
  group: vitistack.io
  names:
    kind: KubernetesProvider
    listKind: KubernetesProviderList
    plural: kubernetesproviders
    shortNames:
    - kp
    singular: kubernetesprovider
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KubernetesProvider is the Schema for the KubernetesProviders
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubernetesProviderSpec defines the desired state of KuberneteProvider
            properties:
              config:
                properties:
                  name:
                    description: The name of the Kubernetes provider
                    type: string
                required:
                - name
                type: object
            required:
            - config
            type: object
          status:
            description: KubernetesProviderStatus defines the observed state of KuberneteProvider
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
