apiVersion: v1
kind: ConfigMap
metadata:
  name: vitistack-config
data:
  # Required configuration keys for the vitistack-operator
  name: "test-stack"
  provider: "test-provider"
  description: "Testing viti stack"
  region: "test-region"
  location: "test-country"
  zone: "test-zone"
